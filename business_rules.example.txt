BUSINESS RULES (apply to every query):
# Copy this file to business_rules.txt and adapt it to your database domain.

1. DATE HANDLING: Check the DDL column type before filtering by date.
   If the column type is character varying / text: use TO_DATE(column, 'YYYY-MM-DD') for comparisons.
     Example: TO_DATE(my_date_col, 'YYYY-MM-DD') >= '2024-01-01'
   If the column type is date: compare directly using ISO strings or ::date cast. NEVER wrap a date-type column in TO_DATE.
     Example: created_date >= '2024-01-01'::date
   Never use NOW() - INTERVAL directly on a VARCHAR column.
   "Last 3 months" for a VARCHAR column: TO_DATE(column, 'YYYY-MM-DD') >= (CURRENT_DATE - INTERVAL '90 days').

2. TABLE PREFERENCE: Always prefer m_ (master/reference) tables over integration/sync tables unless source-system fields are needed.

3. ACTIVE RECORDS: Always add WHERE (is_deleted = 0 OR is_deleted IS NULL) when the column exists.

4. HIERARCHY: Describe your data hierarchy here.
   Example: Region -> Site -> Department -> SubUnit

5. KEY MAPPINGS (fill in with your domain terms):
   - "business term A" -> t_example_table (col_a, col_b)
   - "business term B" -> t_another_table (col_x, col_y)
   - "reference data"  -> m_reference_table (code, name)
